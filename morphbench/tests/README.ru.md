# Проверки

English version of this file — [README.md](README.md).

```
python tests/run.py                 все наборы подряд, одна итоговая строка в конце
python tests/run.py test_tri.py     один набор, по образцу имени файла
python tests/test_layers.py         любой файл запускается и сам по себе
```

Только `unittest` из стандартной библиотеки: стороннего фреймворка проверок нет. Проверки лежат
**вне** пакета `morphbench\`, он их не импортирует; корень программы они находят от собственного
расположения.

Язык закреплён: `run.py` и `common.py` оба выставляют `MORPHBENCH_LANG=en`, поэтому проверки
читают английские тексты, на каком бы языке ни говорила машина. Без этого проверка, сверяющая
сообщение слово в слово, у одного человека проходила бы, а у другого падала.

Ни один набор не читает файл из сборки модов: тело собирается в памяти из `Shape`, `Bone`,
`BodyModel`, `Morph`, `MorphSet` (общие сборщики — в `common.py`, вывернутый наружу замкнутый куб —
в `solids.py`) и подключается к фасаду через `MorphBench.attach`. Настройки создаются во временной
папке (`Config(<tmp>/morphbench.json)`), поэтому настоящий `morphbench.json` не трогается; внутри
проверок `mb.py` получает фасад с теми же настройками подменой `mb.MorphBench`. Запуск под MO2
изображается подменой `Environment.inside_mo2`, а игры из реестра — подменой
`Environment.game_roots`: ни одной настоящей папки сборки нигде.

| Набор | Нужен PyNifly | Что проверяет |
|---|---|---|
| `test_tri.py` | да | настоящий TRIP, записанный `TripFile`, читается обратно через `MorphSet.from_file` с точностью до кванта int16; FRTRI, записанный `TriFile` |
| `test_nif.py` | да | крошечный NIF, сделанный PyNifly, читается обратно через `BodyModel.from_nif`; `open()` находит лежащий рядом `.tri` |
| `test_morphs.py` | нет | пустой морф, объявленный, но отсутствующий, `apply` с долей и с номерами вершин за концом меша |
| `test_strain.py` | нет | растяжение рёбер на сетке из двух треугольников, посчитанное руками; порог; сдвиг фигуры целиком даёт ноль |
| `test_layers.py` | нет | насколько близко оболочка сидит над кожей, которая двигается, и над той, которая нет; `Proximity`; `contactRadius` и `minContact` из настроек |
| `test_bounds.py` | частично | шары охвата: наименьший шар над облаком точек, состояния ползунков, строки фасада и запись — разложенный руками блок фигуры и настоящий NIF от PyNifly обязаны сойтись в раскладке |
| `test_facade.py` | нет | таблицы фасада совпадают с `as_dict()` объектов `Analyzer`; JSON без numpy; ползунки равны `Morph.apply`; флаги раскраски |
| `test_focus.py` | частично | `sphere_of`, наведение на кость / на морф / на фигуру, `framing`, `focus_targets`; одна проверка пишет NIF ради `open()` |
| `test_framing.py` | нет | `MorphBench.framing()`: центр и полуразмер в осях камеры (поворот 0 и 90 — руками), только видимые фигуры, ползунки, наведение, умноженное на `focusPadding`, сдвиг кадра, фигура без треугольников не в счёт, всё скрыто — `RuntimeError` |
| `test_normals.py` | нет | `vertex_normals`: сетка против часовой стрелки смотрит вверх, обратная намотка — вниз, куб — наружу, одинокая вершина даёт (0,0,1), единичная длина, вес по площади; фасад считает их по сдвинутым вершинам |
| `test_light.py` | нет | свет в `ViewState`: режим из настроек, вектор, едущий с камерой или стоящий в мире, направление, заданное в текущем режиме, нулевой вектор, зажатые силы, `light_state` без numpy; растеризатор на кубе: перед и зад одинаково ярки при свете за камерой и по-разному — при мировом, `fill` поднимает тень, `flat` красит грань одним цветом |
| `test_zoom.py` | нет | `zoom_at`: точка сцены под курсором остаётся на месте, сверено с собственными формулами растеризатора (до 1e-3 пикселя, на неквадратном холсте), зажим ниже 0.05, без кадра меняется только масштаб, фасад сам вычисляет кадр |
| `test_chains.py` | нет | цепочки костей для качающейся физики: имя звена, порядок вниз по дереву, кожа, разделённая по звеньям, обрывы в цепочке, передача цепочки движку |
| `test_colliders.py` | частично | коллайдеры: капсула, посадка её на кожу, набор тел, побайтовая правка файла и слой PPB; разбор заголовка обязан сойтись с настоящим NIF |
| `test_physics.py` | нет | качающаяся физика от начала до конца: цепочки, отданные движкам, капсулы по звеньям, слои `smp` и `cbpc`, команда `physics`; цепочка, не отданная никому, в вывод не попадает, а в заголовке перечислены все |
| `test_catalog.py` | частично | `Catalog` над деревом пустых файлов: суффиксы веса, регистр букв, формат по заголовку, подпапки, `get` по номеру / имени / подстроке, кеш и `rescan`; `Environment` под изображённой MO2 и изображёнными играми; `catalog` / `open_entry` фасада (одна проверка на настоящем NIF) |
| `test_cli.py` | частично | `mb.py env --json`, `catalog --json/--all/--find`, `serve --status/--stop`; `render` с `--zoom-at`, `--light`, `--light-dir`, `--light-power` на настоящем NIF |
| `test_serve.py` | да | `presenters.serve`: сервер в фоновом потоке на порту 0, `/api/environment`, `/api/catalog`, `/api/payload`, страница со своим холстом, 400/403/404 в виде JSON, 403 под изображённой MO2, `stop()` освобождает порт; пропускается, пока модуля нет |
| `test_serve_root.py` | нет | `serve`, поднятый вообще без корня: страница выходит с пустым списком, корень называют потом через `/api/root`, а `ServerLink` отличает свободный порт, наш сервер и чужую программу |
| `test_web.py` | нет | страница `presenters.web`, собранная из верстака в памяти: `<canvas`, имена, никаких внешних ссылок; пропускается, пока модуля нет |
| `test_text.py` | нет | таблица и сводка слоя `presenters.text` |
| `test_journal.py` | нет | журнал: уровни решают, что писать, приёмники — куда; мёртвый приёмник не останавливает живой и не бросает наружу — та самая поломка, что однажды оборвала клиента без ответа |
| `test_i18n.py` | нет | каталог сообщений: ключ без текста и в выбранном, и в базовом языке, подстановка по месту вместо подстановки по имени, пропавший или сломанный файл языка, разошедшиеся между языками ключи, ключ, случайно оставшийся английской фразой |

PyNifly ищется ровно так, как его ищет программа: ключ `pynifly` в настройках, иначе аддон
`io_scene_nifly` в папке аддонов Blender. Без него наборы, которым надо записать настоящий файл,
**пропускаются** с названной причиной — пропуск не считается отказом. Отказ самого API PyNifly
при создании файла — тоже пропуск, и он несёт текст исключения: проверяется чтение верстака,
а не PyNifly.
