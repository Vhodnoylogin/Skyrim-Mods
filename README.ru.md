# Skyrim Mods

Наши разработки для Skyrim: моды, инструменты и службы. Один репозиторий,
**ветка на каждую разработку**.

*This page in English: [README.md](README.md).*

| Ветка | Что это |
|---|---|
| `mo2aibridge` | **MO2 ApI Bridge** — локальный HTTP-мост к работающей Mod Organizer 2, чтобы скрипты и ИИ-агенты читали и правили сборку, не кликая по её окну |
| `envoy` | **Envoy Framework** — шина сообщений SKSE: сам мост, голосовой адаптер и тестовый слушатель |
| `voice` | голосовая служба: распознавание и синтез речи |
| `morphbench` | **morphbench** — верстак для мешей и именованных морфов вне игры: читает то, что собрал BodySlide, и говорит числами, держатся ли морфы, шары охвата, капсулы столкновений и настройки физики |
| `master` | ствол: эти правила, общий список игнорирования и доведённые до конца модули |

**Каждый модуль описывает себя сам.** Открой его ветку и начинай с `README.md` модуля:
у каждого модуля английский `README.md` лежит рядом с русским `README.ru.md`, и так на каждом
его уровне, плюс манифест `CLAUDE.md` — он обращён к ИИ-помощнику, которому папка досталась
без истории за спиной. Подробная документация живёт рядом с кодом, который описывает, а не здесь.

## Как устроены ветки

Каждый модуль живёт на своей ветке, постоянно выписанной в собственную рабочую копию:

| Папка | Ветка | Что это |
|---|---|---|
| `dev\repo\` | `master` | ствол, здесь физически лежит `.git` |
| `dev\envoy\` | `envoy` | Envoy Framework: мост, голосовой адаптер и мод-модель |
| `dev\voice\` | `voice` | голосовая служба: распознавание и синтез речи |
| `dev\morphbench\` | `morphbench` | верстак morphbench |
| `dev\wt-mo2aibridge\` | `mo2aibridge` | плагин MO2 ApI Bridge |
| `dev\wt\<задача>\` | временная | ветка под одну задачу внутри модуля |

Такая раскладка даёт главное: в один коммит **не могут** попасть файлы двух разных модов -
на ветке модуля файлов соседа просто нет.

## Как завести временную копию под задачу

```
git -C dev\repo worktree add -b envoy-protocol ..\wt\envoy-protocol envoy
git -C dev\wt\envoy-protocol commit -m "..."
git -C dev\envoy merge envoy-protocol
git -C dev\repo worktree remove ..\wt\envoy-protocol
```

Одну и ту же ветку нельзя выписать в двух копиях сразу - git откажет, и это защита,
а не помеха.

## Ветка наследует ствол, и это нормально

Новая ветка ответвляется от `master`, поэтому в её рабочей копии оказывается всё готовое,
что на стволе уже лежит. **Удалять его нельзя.** Коммит с удалением - обычное изменение:
слитый в ствол, он унесёт эти файлы и оттуда, а «чистая» ветка окажется отложенной порчей
мастера.

Правило «на ветке модуля файлов соседа нет» относится к тому, что на ветке **добавляют
и правят**, а не к тому, что досталось от ствола. Понадобилось изменить саму раскладку -
это решение об общей истории, и принимает его пользователь, а не ветка.

## Свежий клон

Модули в работе появляются после `git worktree add` под каждую ветку.
