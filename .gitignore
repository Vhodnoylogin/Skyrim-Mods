# Продукты сборки и окружения в репозиторий не попадают.
build/
dist/
.venv/
models/
__pycache__/
*.pyc

# Логи, токены и локальные настройки - у каждой машины свои.
*.log
*-token.txt
build.local.json

# Черновики прогонов.
session.jsonl
*.wav
*.bak-*
