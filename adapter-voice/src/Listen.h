#pragma once

#include "Config.h"

namespace Voice
{
	// Опрос одной модели: подключиться к её службе или поднять её, затем без
	// конца забирать распознанное и переносить в мост. Живёт в своём потоке
	// до конца процесса игры.
	void PollModel(const Model& a_model);
}
