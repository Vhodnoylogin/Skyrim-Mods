#pragma once

namespace Envoy
{
	// Окно моста в игровом меню модов.
	//
	// Мост показывает его через SKSE Menu Framework - чужой мод, который рисует
	// общую панель и пускает в неё плагины. Зависимость **мягкая**: заголовок
	// ничего не линкует, а функции ищет в уже загруженной чужой библиотеке.
	// Нет этого мода - нет и окна, и мост об этом даже не узнает.
	//
	// Окно меняет только текущую сессию. Постоянное значение задаётся в файле
	// настроек: так же устроено переключение источника у адаптеров, и по той же
	// причине - меню под рукой, но следующий запуск не должен зависеть от того,
	// что человек когда-то нажал.
	class MenuPanel
	{
	public:
		static void Install();
	};
}
